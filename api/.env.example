# Copy to api/.env on server (never commit .env)

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zarkorea_zarkorea
DB_USERNAME=zarkorea_user
DB_PASSWORD=your_strong_password_here

# Set true only while debugging (shows error message in JSON)
APP_DEBUG=false
